# منصة كروت المعايدة - Greeting Cards Platform

منصة ويب احترافية لإنشاء ومشاركة كروت المعايدة للمناسبات العربية.

## المميزات

- ✨ قوالب احترافية لجميع المناسبات
- 🎨 محرر متقدم باستخدام Fabric.js
- 📝 مكتبة رسائل نصية جاهزة
- 👥 نظام مستخدمين كامل
- 🔐 لوحة تحكم إدارية
- 📱 تصميم متجاوب

## المتطلبات

- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث
- Apache مع mod_rewrite
- GD Library للصور

## التثبيت

### 1. رفع الملفات

ارفع جميع الملفات إلى السيرفر الخاص بك (cPanel/VPS).

### 2. إنشاء قاعدة البيانات

1. افتح phpMyAdmin
2. أنشئ قاعدة بيانات جديدة
3. استورد ملف `database.sql`

### 3. إعداد الاتصال

عدل ملف `config/config.php`:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'your_username');
define('DB_PASS', 'your_password');
define('DB_NAME', 'your_database');
define('SITE_URL', 'https://yourdomain.com');
```

### 4. صلاحيات المجلدات

تأكد من أن مجلد `uploads/` قابل للكتابة:

```bash
chmod 755 uploads/
chmod 755 uploads/templates/
```

### 5. تسجيل الدخول للإدارة

- الرابط: `https://yourdomain.com/admin/`
- البريد: `admin@example.com`
- كلمة المرور: `admin123`

**مهم:** غير كلمة المرور فوراً بعد أول تسجيل دخول!

## الهيكل

```
greeting-cards-php/
├── config/              # ملفات الإعداد
├── includes/            # Header, Footer, Functions
├── admin/               # لوحة التحكم الإدارية
├── assets/              # CSS, JS, Images
├── uploads/             # الملفات المرفوعة
├── index.php            # الصفحة الرئيسية
├── templates.php        # صفحة القوالب
├── editor.php           # المحرر
├── login.php            # تسجيل الدخول
├── register.php         # حساب جديد
└── database.sql         # قاعدة البيانات
```

## الاستخدام

### إضافة قوالب جديدة

1. سجل دخول للوحة التحكم
2. اذهب إلى "إدارة القوالب"
3. ارفع صورة القالب (800x600 موصى به)
4. اختر القسم المناسب

### إدارة الأقسام

يمكنك إضافة/تعديل الأقسام من قاعدة البيانات مباشرة أو من لوحة التحكم.

## الأمان

- جميع كلمات المرور مشفرة بـ bcrypt
- حماية من SQL Injection
- حماية من XSS
- CSRF Tokens
- تحقق من أنواع الملفات المرفوعة

## الدعم الفني

للمساعدة أو الإبلاغ عن مشاكل، تواصل معنا.

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

تم البناء بـ ❤️ باستخدام PHP و MySQL و Fabric.js
# greetingcards
